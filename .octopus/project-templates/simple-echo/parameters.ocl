parameter "Team Name" {
    display_settings = {
        Octopus.ControlType = "SingleLineText"
    }
    help_text = "This value is baked into the template and cannot be changed by template users."
    label = "The team that owns this project."

    value "OctoBarc" {}
}

parameter "Worker Pool" {
    display_settings = {
        Octopus.ControlType = "WorkerPool"
    }
    help_text = ""
    label = ""
}